let overEighteen = isOverEighteen(age);

function isOverEighteen(age) {    // Function requires a variable to be passed in as an input parameter
    if (age >= 18) {                // Comparison operator checks value of input parameter
      return true;
    } else {
      return false;
    }
  }

  function updatePage(){
      
  }